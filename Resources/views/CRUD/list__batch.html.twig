{% extends '@SonataAdmin/CRUD/list__batch.html.twig' %}

{% block field %}
    {% spaceless %}
        {% if admin.canBeBatched is not defined or admin.canBeBatched(object) %}
            {{ parent() }}
        {% endif %}
    {% endspaceless %}
{% endblock %}
